<h2>商品一覧</h2>全<%= @items.count %>件

<% @items.each do |item| %>

  <%= link_to item_path(item.id) do %>
  <%= image_tag item.image, size: "100x100" %>
  <% end %>

 <%= item.name %>

 ￥<%= item.price %>

<% end %>  